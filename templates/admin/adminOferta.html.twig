{% extends 'barraMenu.html.twig' %}

{% block title %}Hello AdminOfertaController!{% endblock %}

{% block body %}

    <table class="table table-hover">
        <thead>
            <tr>
            <th scope="col">Id</th>
            <th scope="col">Titulo</th>
            <th scope="col">Descripcion</th>
            <th scope="col">Fecha</th>
            <th scope="col">Empresa</th>
            <th scope="col"><button type="button" class="btn btn-primary">Añadir</button></th>
            </tr>
        </thead>
        <tbody>
            {% for oferta in ofertas %}
                <tr>
                <th scope="row">{{oferta.id}}</th>
                <td>{{oferta.titulo}}</td>
                <td>{{oferta.descripcion}}</td>
                <td>Fecha Pendiente</td>
                <td>Empresa Pendiente</td>
                <th scope="col">
                    <button type="button" class="btn btn-primary">Modificar</button>
                    <button type="button" class="btn btn-primary">Eliminar</button>
                </th>
                </tr>
            {% endfor %}
            
        </tbody>
    </table>
    
{% endblock %}
